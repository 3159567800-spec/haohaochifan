services:
  - type: web
    name: haohaochifan
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -b 0.0.0.0:$PORT
    plan: free
    envVars:
      - key: FLASK_DEBUG
        value: "0"
